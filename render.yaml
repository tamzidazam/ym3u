services:
  - type: web
    name: yt-m3u8-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: API_KEY
        value: ""
      # COOKIES_DIR defaults to ./data next to main.py — no disk needed
